server:
  port: 8081
  wait-time-in-ms-when-sync-empty: 3000

spring:
  spring:
    profiles:
      active: native
  data:
    mongodb:
      uri: mongodb+srv://admin:admin@cluster0.qrm8sxv.mongodb.net/?retryWrites=true&w=majority
      database: cliente
      auto-index-creation: true

application-version: 1.0